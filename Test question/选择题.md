# Dart 基础选择题（第1~70课）

> 覆盖 SUMMARY.md 全部课程 | ?共112题?
## 使用说明

- 建议先独立完成，再对照文末「答案与解析?- 代码题默认在 **null-safe Dart 3** 环境?- ?第41~45 课内容参考对?`lesson41.dart` ~ `lesson45.dart`（SUMMARY 中未单独成节?
---

## 第一阶段：基础语法（第 第1~9课）

### ?1 课：第一个程?
**1.** Dart 程序的入口函数是?
A. `main()`  
B. `void main()`  
C. `start()`  
D. `run()`

**2.** 下列哪条语句写法正确?
A. `print('Hello')`  
B. `Print('Hello')`  
C. `print('Hello')` 后面不加分号也能编译  
D. `console.log('Hello')`

### ?2 课：变量

**3.** `var x = 10;` ?`x` 的类型是?
A. `dynamic`  
B. `int`  
C. `num`  
D. 编译器无法推?
**4.** 字符串插?`'面积: ${width * height}'` 中，为什么表达式要加 `{}`?
A. 表示可选参? 
B. 区分变量名和表达式边? 
C. 表示 Record 类型  
D. 必须?`{}` 才能输出字符?
### ?3 课：常量 final ?const

**5.** 下列哪段代码能正常编译？

A. `const x = DateTime.now();`  
B. `final x = DateTime.now();`  
C. `const x; x = 1;`  
D. `final pi = 3.14; pi = 3.14159;`

**6.** `const` ?`final` 的核心区别是?
A. `const` 可以在运行时赋? 
B. `const` 的值必须在编译时确? 
C. `final` 可以多次赋? 
D. 两者完全相?
### ?4 课：运算?
**7.** `print(7 / 2); print(7 ~/ 2);` 的输出是?
A. `3.5` ?`3.5`  
B. `3` ?`3`  
C. `3.5` ?`3`  
D. `3` ?`3.5`

**8.** `10 / 3` 的结果类型是?
A. `int`  
B. `double`  
C. `num`  
D. `dynamic`

### ?5 课：条件判断 if / else

**9.** 三元运算?`age >= 18 ? '成年' : '未成?` 等价于？

A. 只有 if，没?else  
B. if-else 的简写形? 
C. switch 表达? 
D. 循环语句

**10.** `if (score >= 60)` 中括号里的表达式类型必须是？

A. `int`  
B. `String`  
C. `bool`  
D. 任意类型

### ?6 课：循环

**11.** 遍历 List 最简洁的写法是？

A. `for-in`  
B. 只能?`while`  
C. 只能用传?`for (int i=0; ...)`  
D. `do-while`

**12.** 不知道循环次数、由条件控制时，优先选？

A. `for-in`  
B. `while`  
C. `for (int i=0; i<n; i++)`  
D. `switch`

### ?7 课：函数

**13.** 箭头函数 `int square(int n) => n * n;` 的限制是?
A. 可以写多行逻辑  
B. 只能写一行表达式  
C. 不能有参? 
D. 不能返回?
**14.** 无返回值函数应声明为？

A. `null`  
B. `void`  
C. 不写返回类型（推荐）  
D. `Object`

### ?8 课：List 列表

**15.** List 取第一个元素的索引是？

A. `1`  
B. `0`  
C. `-1`  
D. 取决?List 类型

**16.** `fruits.contains('苹果')` 返回的类型是?
A. `int`  
B. `String`  
C. `bool`  
D. `void`

### ?9 课：Map 键值对

**17.** Map 通过什么方式取值？

A. 数字索引 `[0]`  
B. ?`[key]`  
C. 只能 `.get()`  
D. 只能遍历不能取?
**18.** `scores.forEach((subject, score) { ... })` 中第一个参数是?
A. 值（value? 
B. 键（key? 
C. 索引  
D. MapEntry 对象

---

## 第二阶段：面向对象（?第10~20课）

### ?10 课：类与对象

**19.** `Animal(this.name, this.age)` ?`this.name` 的作用是?
A. 调用父类方法  
B. 区分参数名和对象属? 
C. 声明静态变? 
D. 创建新对?
**20.** 创建对象使用的关键字是？

A. `new`（Dart 3 中必须写? 
B. 直接调用构造函数，?`Animal('旺财', 3)`  
C. `create`  
D. `instanceof`

### ?11 课：命名参数与默认?
**21.** 命名参数的语法是?
A. `[String name]`  
B. `{required String name}`  
C. `(String name)`  
D. `<String name>`

**22.** 可选位置参?`[String greet = '你好']` 调用时？

A. 必须写参数名  
B. 按顺序传值，可省? 
C. 不能传默认? 
D. 只能传一个参?
### ?12 课：空安?
**23.** `String? name;` 表示?
A. name 不能?null  
B. name 可以?null  
C. name ?dynamic 类型  
D. name 必须立即赋?
**24.** `print(nickname ?? '默认昵称');` ?`??` 的含义是?
A. 逻辑? 
B. 左边?null 时用右边的? 
C. 安全调用  
D. 类型转换

**25.** `user?.length` ?`user` ?null 时返回？

A. `0`  
B. 抛出异常  
C. `null`  
D. 编译错误

### ?13 课：命名构造函?
**26.** 命名构造函数的调用方式是？

A. `Person('小明', 25)`  
B. `Person.guest()`  
C. `new Person()`  
D. `Person::guest()`

**27.** `Person.guest() : this('访客', 18);` ?`: this(...)` 是？

A. 继承父类  
B. 初始化列表，转发给另一个构造函? 
C. 断言  
D. 延迟初始?
### ?14 课：继承

**28.** `class Dog extends Animal` ?`extends` 表示?
A. 实现接口  
B. 继承父类代码  
C. 混入 Mixin  
D. 泛型约束

**29.** 子类调用父类构造函数用?
A. `this(...)`  
B. `super(...)`  
C. `parent(...)`  
D. `extends(...)`

### ?15 课：级联运算?..

**30.** `list..add(1)..add(2)` ?`..` 返回的是?
A. `add()` 的返回? 
B. `list` 本身  
C. `void`  
D. 新创建的 List

### ?16 课：抽象?
**31.** 抽象类能否直?`new`?
A. 可以  
B. 不可以，编译报错  
C. 只有加了 factory 才可? 
D. 只有子类可以 new 父类

**32.** `@override` 的主要作用是?
A. 提高运行速度  
B. 标记重写，父类无此方法时编译报错  
C. 声明抽象方法  
D. 声明静态方?
### ?17 课：implements 接口

**33.** `implements` ?`extends` 的区别是?
A. `implements` 继承代码，`extends` 签合? 
B. `implements` 必须自己实现所有方法，`extends` 可继承实? 
C. 两者完全相? 
D. `implements` 只能用于 enum

### ?18 课：Mixin 混入

**34.** 使用 Mixin 的关键字是？

A. `extends`  
B. `implements`  
C. `with`  
D. `mixin extends`

**35.** Mixin 适合的场景是?
A. 严格?is-a 父子关系  
B. 多个类共享同一种能力，但不是父子关? 
C. 替代泛型  
D. 替代 enum

### ?19 课：泛型

**36.** `List<String>` 存入 `int` 值会怎样?
A. 运行时自动转? 
B. 编译报错  
C. 存入后变?dynamic  
D. 静默失败

**37.** 泛型?`dynamic` 的主要区别是?
A. 没有区别  
B. 泛型编译时检查类型安? 
C. dynamic 更安? 
D. 泛型只能用于 List

### ?20 课：枚举

**38.** `Weekday.wed.index` 返回的是?
A. 枚举名称字符? 
B. ?0 开始的序号  
C. 总是 1  
D. null

**39.** 枚举相比魔法字符串的主要优势是？

A. 可以存任意? 
B. 编译器检查拼写，switch 可全覆盖  
C. 性能更差但更安全  
D. 可以用中文作为枚举值名

---

## 第三阶段：现?Dart 特性（?21~30 课）

### ?21 课：异步 async / await

**40.** `await` 的作用是?
A. 阻塞整个 CPU  
B. 等待 Future 完成，不阻塞主线程做其他? 
C. 取消异步操作  
D. 创建?Isolate

**41.** 异步函数必须标记为？

A. `sync`  
B. `async`  
C. `yield`  
D. `late`

### ?22 课：Stream ?
**42.** Stream ?Future 的核心区别是?
A. Stream 返回一次，Future 返回多次  
B. Stream 返回多次，Future 返回一? 
C. 两者完全相? 
D. Stream 只能用于文件

**43.** 消费 Stream 用？

A. `await future`  
B. `await for`  
C. `for-in`（同步，不能用于 Stream? 
D. `yield`

### ?23 课：异常处理 try / catch

**44.** 没有 try-catch 时，未捕获的异常会？

A. 被自动忽? 
B. 导致程序崩溃  
C. 转为 Future  
D. 进入 Zone

### ?24 课：Extension 扩展方法

**45.** Extension 的主要用途是?
A. 修改 String、int 等已有类型的源码  
B. 给已有类型添加新方法，无需工具函数  
C. 替代继承  
D. 创建新类?
### ?25 课：Record 记录

**46.** 命名 Record `(name: '小红', score: 92)` 取值用?
A. `$1`、`$2`  
B. `.name`、`.score`  
C. `['name']`  
D. `.get('name')`

**47.** Record 相比 Map 的优势是?
A. 更灵活，键可以是任意字符? 
B. 类型安全，字段名编译时确? 
C. 可以有方? 
D. 必须定义 class

### ?26 课：sealed class 密封?
**48.** sealed class 相比普?abstract class 的主要优势是?
A. 可以在任意文件继承子? 
B. switch 必须覆盖所有子类，编译器检? 
C. 不能有多个子? 
D. 只能用于 enum

**49.** sealed class 的子类定义范围是?
A. 任意文件  
B. 只能?sealed class 同一文件? 
C. 只能?package ? 
D. 只能?test 目录

### ?27 课：集合常用方法

**50.** `nums.where(...).map(...)` 返回的类型通常是？

A. `List`  
B. `Iterable`  
C. `Map`  
D. `Stream`

**51.** `reduce` ?`fold` 的区别是?
A. `reduce` 需要初始值，`fold` 不需? 
B. `fold` 可以指定初始值，`reduce` 用第一个元素作初始? 
C. 完全相同  
D. 只能用于 Map

### ?28 课：factory 工厂构造函?
**52.** factory 构造函数的特点是？

A. 每次一定创建新对象  
B. 可以返回已有对象或子类实? 
C. 不能访问 static 成员  
D. 必须?const

**53.** `_internal` 私有构造函数配?factory 常用于？

A. 运算符重? 
B. 单例、缓存、对象池  
C. Mixin  
D. 泛型约束

### ?29 课：static 静态成?
**54.** 静态成员通过什么方式调用？

A. `对象.成员`  
B. `类名.成员`  
C. `this.成员`  
D. `super.成员`

### ?30 课：getter ?setter

**55.** `print(r.area)` 调用的是?
A. 普通方?`area()`  
B. getter，像属性一样访? 
C. 静态方? 
D. 构造函?
---

## 第四阶段：进阶特性（?31~40 课）

### ?31 课：late 延迟初始?
**56.** `late` 变量的初始化时机是？

A. 创建对象? 
B. 第一次访问时  
C. 程序启动? 
D. 编译?
### ?32 课：assert 断言

**57.** `assert` 默认在什么环境生效？

A. 生产环境  
B. 调试模式（需 `--enable-asserts`? 
C. 永远生效  
D. 只在 Web 平台

**58.** 用户输入校验应使用？

A. `assert`  
B. `if + throw`  
C. `print`  
D. `late`

### ?33 课：模式匹配解构

**59.** Map 解构 `var {'name': name}` ?Record 解构的主要区别是?
A. 完全相同  
B. Map 键是运行时字符串，Record 字段名是编译时标识符  
C. Map 不能解构  
D. Record 必须?`$1`

### ?34 课：运算符重?
**60.** 重写 `==` 时必须同时重写？

A. `toString`  
B. `hashCode`  
C. `copyWith`  
D. `runtimeType`

### ?35 课：typedef 类型别名

**61.** `typedef Filter = bool Function(int age);` 的作用是?
A. 定义新类  
B. 给复杂函数类型起别名  
C. 创建泛型约束  
D. 声明全局变量

### ?36 课：callable class 可调用的?
**62.** 让对象像函数一样调用，需要定义？

A. `main` 方法  
B. `call` 方法  
C. `run` 方法  
D. `invoke` 方法

### ?37 课：import 导入

**63.** 下列哪个?*不需?*手动 import?
A. `dart:math`  
B. `dart:core`  
C. `dart:io`  
D. `package:http/http.dart`

### ?38 课：copyWith 不可变更?
**64.** `copyWith({int? age})` ?`age ?? this.age` 的含义是?
A. age ?0 时用原? 
B. 没传 age（null）时保留?age  
C. 总是用新 age  
D. 抛出异常

### ?39 课：Isolate 并发

**65.** Isolate ?async/await 的主要区别是?
A. Isolate 在同一线程，async 在新线程  
B. Isolate 在新线程，能利用多核；async 在同一线程  
C. 完全相同  
D. Isolate 只能用于 Web

**66.** Isolate ?JS async 的区别是?
A. JS async 是真多线? 
B. Isolate 是真多线程（?CPU 核），JS async 是单线程排队  
C. 没有区别  
D. Isolate 不能并行

### ?40 课：extension type 扩展类型

**67.** `extension type UserId(int value)` 的特点是?
A. 有运行时对象开销  
B. 零开销编译时包装，外部不能直接?int ?UserId  
C. 可以添加可变字段  
D. 等同?class

**68.** extension type、class、extension 三者中，能给已有类型加方法但不能加字段的是?
A. class  
B. extension type  
C. extension  
D. sealed class

---

## 第五阶段：实用技能（?41~70 课）

### ?41 课：类修饰符

**69.** `final class Robot` 在其他文件中?
A. 可以 extends ?implements  
B. 既不?extends 也不?implements  
C. 只能 implements  
D. 只能 extends

**70.** `base class Dog` 在其他文件中?
A. 可以 extends，不?implements  
B. 可以 implements，不?extends  
C. 两者都可以  
D. 两者都不可?
### ?42 课：Switch 表达?
**71.** Switch 表达式相?switch 语句的优势是?
A. 必须?break  
B. 可以直接返回值，强制穷尽性检? 
C. 不能用于 enum  
D. 只能有一个分?
### ?43 课：if-case 模式匹配

**72.** if-case 适合的场景是?
A. 必须处理所有可能情? 
B. 只关心匹配某一种模? 
C. 替代 for 循环  
D. 替代 try-catch

### ?44 课：for-in ?Iterable

**73.** `where`、`map` 等方法返?Iterable 的特点是?
A. 立即执行并修改原集合  
B. 惰性求值，用到才计? 
C. 总是返回 List  
D. 只能遍历一次且会清空数?
**74.** for-in 循环背后使用的是?
A. 索引 `[i]`  
B. iterator ?`moveNext()` ?`current`  
C. `reduce`  
D. `StreamController`

### ?45 课：Set 集合

**75.** Set ?List 的主要区别是?
A. Set 有序可重? 
B. Set 无序且不重复  
C. Set 只能?int  
D. Set 没有 `add` 方法

### ?46 课：DateTime 日期时间

**76.** `Duration` 能否直接表示? 个月」？

A. 可以，`Duration(months: 1)`  
B. 不可以，Duration 没有 months 参数  
C. 可以，用 `Duration(days: 30)` 等价  
D. 只能?`DateTime.addMonth()`

**77.** 判断两个 DateTime 是否是「同一天」应该？

A. 直接?`==`  
B. 比较 year、month、day  
C. 比较 hour ?minute  
D. ?`isBefore` 即可

### ?47 课：JSON 编解?
**78.** `jsonDecode` 返回的类型默认是?
A. `Map<String, String>`  
B. `dynamic`，需?`as` 转换  
C. 自动转为自定义类  
D. `String`

### ?48 课：文件读写

**79.** `dart:io` 中的 File 操作?
A. 可在 Web ?Flutter 通用  
B. 只能在非 Web 平台（CLI、服务器、Flutter 移动端等? 
C. 只能?Linux  
D. 不需?async

### ?49 课：正则表达?RegExp

**80.** 创建正则 `\d+` 推荐写法是？

A. `RegExp('\d+')`  
B. `RegExp(r'\d+')`  
C. `RegExp('//d+')`  
D. `new Regex('\d+')`

**81.** 验证手机号格式（11 位? 开头）应使用？

A. `String.contains`  
B. `RegExp.hasMatch`  
C. `substring`  
D. `split`

### ?50 课：排序?Comparable

**82.** 临时改变排序规则（如降序）应?
A. 类必?implements Comparable  
B. `list.sort((a, b) => b.compareTo(a))`  
C. 不能改变，sort 固定升序  
D. 只能?`reversed` getter

### ?51 课：Timer 定时?
**83.** 需要「每?1 秒重复执行且?cancel」应选？

A. `Future.delayed`  
B. `Timer.periodic`  
C. `async/await`  
D. `Isolate.run`

**84.** `Future.delayed` ?`Timer` 的区别是?
A. Timer 不能延迟执行  
B. Future.delayed 不能取消，Timer 可以 cancel  
C. 完全相同  
D. Timer 只能执行一?
### ?52 课：StreamController

**85.** 数据来自外部事件（如按钮点击）时，更适合?
A. `async*` 生成? 
B. `StreamController` 手动 add  
C. `sync*`  
D. `List.add`

**86.** 默认?StreamController 创建的流?
A. 可以被无限次监听  
B. 只能被监听一次（单播? 
C. 不能 close  
D. 自动广播

### ?53 课：增强枚举

**87.** 增强枚举与普通枚举的区别是？

A. 增强枚举可以有字段和方法  
B. 普通枚举可以有构造函数参? 
C. 增强枚举不能?switch  
D. 完全相同

### ?54 课：sync* 同步生成?
**88.** `sync*` 函数返回的类型是?
A. `Stream<T>`  
B. `Iterable<T>`  
C. `List<T>`  
D. `Future<T>`

**89.** `sync*` ?`async*` 的区别是?
A. sync* 同步惰?Iterable，async* 异步 Stream  
B. 完全相同  
C. sync* 返回 Future  
D. async* 不能 yield

### ?55 课：Process 进程

**90.** 执行 `dart --version` 并一次性获取输出，适合?
A. `Process.start`  
B. `Process.run`  
C. `Isolate.run`  
D. `Timer.periodic`

### ?56 课：Uri ?
**91.** 从字符串 `'https://a.com/path?q=1'` 解析 URL 用？

A. `Uri(...)` 构造函? 
B. `Uri.parse(...)`  
C. `jsonDecode`  
D. `RegExp.match`

**92.** `Uri` 类需?import 吗？

A. 需?`import 'dart:uri'`  
B. 不需要，?dart:core ? 
C. 需?`dart:io`  
D. 需要第三方?
### ?57 课：自定义异?
**93.** 自定义可捕获的业务异常应?
A. `extends Error`  
B. `implements Exception`  
C. `extends Object`  
D. 不需要定义类

**94.** `Exception` ?`Error` 的区别是?
A. Error 应捕获处理，Exception 应修代码  
B. Exception 可处理，Error 通常是程?bug  
C. 完全相同  
D. Error 只能用于网络

### ?58 课：Isolate 通信

**95.** 需?Isolate 之间多次收发消息，应使用?
A. `Isolate.run`（一次性）  
B. `SendPort` / `ReceivePort`  
C. 共享全局变量  
D. `static` 成员

### ?59 课：StringBuffer

**96.** 循环拼接 10000 行字符串，性能更好的是?
A. `s += line`  
B. `StringBuffer` + `writeln`  
C. 两者一? 
D. `print` 拼接

### ?60 课：dart:collection

**97.** Dart 默认?`Map()` 实现是？

A. HashMap（无序）  
B. LinkedHashMap（插入顺序）  
C. SplayTreeMap（键排序? 
D. Queue

**98.** `Queue` 适合的场景是?
A. 按键快速查? 
B. 先进先出（排队、任务调度）  
C. 去重  
D. 排序

### ?61 课：NoSuchMethod

**99.** 触发 `noSuchMethod` 通常需要将对象声明为？

A. `final`  
B. `dynamic`  
C. `const`  
D. `static`

### ?62 课：模式匹配进阶

**100.** `var [a, b, ...rest] = list` ?`...rest` 表示?
A. 抛出剩余元素  
B. 收集剩余元素?rest  
C. 删除剩余元素  
D. 编译错误

### ?63 课：注解

**101.** 自定义注解类的构造函数必须是?
A. `factory`  
B. `const`  
C. `async`  
D. 可以有多个非 const 构?
### ?64 课：异步进阶

**102.** `await taskA(); await taskB();` ?`await Future.wait([taskA(), taskB()])` 的区别是?
A. 完全相同  
B. 前者顺序执行耗时不叠加优化，后者并发执? 
C. Future.wait 是顺序的  
D. 前者并发，后者顺?
**103.** `Future.any([sourceA(), sourceB()])` 的含义是?
A. 等所有完? 
B. 谁先完成用谁的结? 
C. 随机选一? 
D. 合并两个结果

### ?65 课：dart:math

**104.** `pow(2, 10)` 的返回值类型是?
A. `int`  
B. `double`  
C. `String`  
D. `num` 但一定是整数

**105.** `Random()` 生成的随机数适合用于?
A. 加密密钥  
B. 抽奖、洗牌、测? 
C. 唯一 ID（绝对不重复? 
D. 哈希密码

### ?66 课：Completer

**106.** Completer 最适合的场景是?
A. 函数内部逻辑清晰的一次?async  
B. 把回调风?API 包装?Future  
C. 替代 Stream  
D. 替代 Timer

**107.** 对同一?Completer 调用两次 `complete()` 会？

A. 覆盖第一次的结果  
B. 报错（只?complete 一次）  
C. 被忽略，无影? 
D. 返回两个 Future

### ?67 课：Stream 进阶

**108.** 单播 Stream ?listen 两次会怎样?
A. 正常，两个监听者都收到数据  
B. 第二?listen 报错  
C. 数据自动变成广播  
D. 第一?listen 被取?
**109.** 只有 Stream 有、Iterable 没有的方法是?
A. `map`  
B. `where`  
C. `timeout`  
D. `take`

### ?68 课：Zone

**110.** Zone 相比 try-catch 的优势是?
A. 只能捕获同步错误  
B. 能捕获整个区域（含异步）的错误，且可传?zoneValues 上下? 
C. 性能更好  
D. 不需?import

### ?69 课：目录操作

**111.** 删除非空目录需要？

A. 直接 `delete()` 即可  
B. `delete(recursive: true)`  
C. ?delete 所?File ?delete Directory  
D. 不能删除非空目录

### ?70 课：泛型进阶

**112.** `double sum<T extends num>(List<T> list)` ?`extends num` 表示?
A. T 可以是任意类? 
B. T 必须?num 的子类型（如 int、double? 
C. T 必须?String  
D. 运行时动态检查，编译不检?
---

## 答案与解?
### 第一阶段

**1. B** ?入口?`void main()`，程序从这里开始执行? 
**2. A** ?Dart ?`print()` 输出；语句以 `;` 结尾；没?`console.log`? 
**3. B** ?`var x = 10` 推断?`int`? 
**4. B** ?简单变量用 `$name`，表达式需 `${表达式}` 区分边界? 
**5. B** ?`DateTime.now()` 运行时才知道，只能用 `final`；A 编译报错，C/D 违反 const/final 规则? 
**6. B** ?`const` 编译时确定；`final` 运行时赋值一次? 
**7. C** ?`/` 保留小数?`3.5`，`~/` 取整?`3`? 
**8. B** ?`/` 运算符结果总是 `double`? 
**9. B** ?三元运算符是 if-else 的简写? 
**10. C** ?if 条件必须?`bool`? 
**11. A** ?遍历集合?`for-in` 最简洁? 
**12. B** ?次数未知、条件控制用 `while`? 
**13. B** ?箭头函数 `=>` 后只能一行表达式? 
**14. B** ?无返回值应声明 `void`；不写会默认?dynamic? 
**15. B** ?List 索引?0 开始? 
**16. C** ?`contains` 返回 `bool`? 
**17. B** ?Map 用键取?`map[key]`? 
**18. B** ?`forEach` 第一个参数是键，第二个是值?
### 第二阶段

**19. B** ?`this.name` 指向对象属性，与参数区分? 
**20. B** ?Dart 3 直接调构造函数，不必?`new`? 
**21. B** ?命名参数?`{}` 包裹? 
**22. B** ?可选位置参数用 `[]`，按顺序传值? 
**23. B** ?`?` 表示可空类型? 
**24. B** ?`??` 空值合并，左边 null 取右边? 
**25. C** ?`?.` 安全调用，对象为 null 时返?null 而不崩溃? 
**26. B** ?命名构造函数格?`类名.名字()`? 
**27. B** ?`: this(...)` 是初始化列表，转发给主构造函数? 
**28. B** ?`extends` 继承父类代码，is-a 关系? 
**29. B** ?子类?`super(...)` 调父类构造? 
**30. B** ?`..` 返回左边对象本身，可链式调用? 
**31. B** ?抽象类不能实例化? 
**32. B** ?`@override` 防手误，父类无此方法时报错? 
**33. B** ?`implements` 必须实现全部方法；`extends` 可继承实现? 
**34. C** ?Mixin ?`with` 混入? 
**35. B** ?共享能力但非父子关系时用 Mixin? 
**36. B** ?泛型在编译时检查，`List<String>` 不能?int? 
**37. B** ?泛型编译期安全，dynamic 运行时才检查? 
**38. B** ?`index` 是从 0 开始的序号? 
**39. B** ?枚举防拼写错误，switch 可全覆盖检查?
### 第三阶段

**40. B** ?`await` ?Future 完成，不阻塞主线程? 
**41. B** ?异步函数标记 `async`? 
**42. B** ?Future 一次，Stream 多次? 
**43. B** ?Stream ?`await for` 消费? 
**44. B** ?未捕获异常导致程序崩溃? 
**45. B** ?Extension 给已有类型加方法? 
**46. B** ?命名 Record ?`.字段名`；位?Record ?`$1`? 
**47. B** ?Record 类型安全；Map 键是动态字符串? 
**48. B** ?sealed class 强制 switch 覆盖所有子类? 
**49. B** ?sealed 子类只能在同一文件定义? 
**50. B** ?链式调用返回 `Iterable`，打印为圆括号? 
**51. B** ?`fold` 可指定初始值；`reduce` 用首元素? 
**52. B** ?factory 可返回缓存对象或子类? 
**53. B** ?factory + 私有构造用于单?缓存? 
**54. B** ?static ?`类名.成员` 调用? 
**55. B** ?getter 像属性访问，无括号?
### 第四阶段

**56. B** ?`late` 第一次访问时才初始化? 
**57. B** ?assert 默认关闭，需 `--enable-asserts`? 
**58. B** ?外部输入?if-throw；assert 用于内部不变量? 
**59. B** ?Map 键运行时确定，Record 字段编译时确定? 
**60. B** ?重写 `==` 必须同时重写 `hashCode`，否?Set/Map 去重出错? 
**61. B** ?typedef 给复杂类型起别名? 
**62. B** ?定义 `call` 方法使对象可调用? 
**63. B** ?`dart:core` 自动导入，含 print、String、Future 等? 
**64. B** ?参数未传时为 null，`??` 保留原值? 
**65. B** ?Isolate 新线程多核；async 同线程? 
**66. B** ?Isolate 真多线程；JS async 单线程事件循环? 
**67. B** ?extension type 零开销，int 不能直接?UserId? 
**68. B** ?extension 只能加方法；extension type 可包装；class 有运行时对象?
### 第五阶段

**69. B** ?`final class` 跨文件不?extends 也不?implements? 
**70. A** ?`base class` ?extends，不?implements? 
**71. B** ?Switch 表达式返回值且强制穷尽? 
**72. B** ?if-case 只匹配一种模式，不必全覆盖? 
**73. B** ?Iterable 方法惰性求值? 
**74. B** ?for-in 底层?iterator? 
**75. B** ?Set 无序不重复；List 有序可重复? 
**76. B** ?Duration 只有 days/hours/minutes 等，?months? 
**77. B** ?`==` 比较时刻；同一天需?year/month/day? 
**78. B** ?jsonDecode 返回 dynamic，需 as 转换? 
**79. B** ?dart:io 不支?Web? 
**80. B** ?原始字符?`r''` 避免 `\d` 被转义? 
**81. B** ?格式验证?RegExp.hasMatch? 
**82. B** ?临时排序规则?Comparator ?sort? 
**83. B** ?重复执行且可取消?Timer.periodic? 
**84. B** ?Future.delayed 不能 cancel；Timer 可以? 
**85. B** ?外部事件驱动?StreamController 手动 add? 
**86. B** ?默认单播，只?listen 一次；广播需 broadcast()? 
**87. A** ?增强枚举可有字段、方法、构造函数? 
**88. B** ?sync* 返回 Iterable? 
**89. A** ?sync* 同步 Iterable；async* 异步 Stream? 
**90. B** ?短命令一次性输出用 Process.run? 
**91. B** ?解析已有 URL 字符串用 Uri.parse? 
**92. B** ?Uri ?dart:core，无需 import? 
**93. B** ?业务异常 implements Exception? 
**94. B** ?Exception 可捕获处理；Error 通常?bug? 
**95. B** ?多次通信?SendPort/ReceivePort；Isolate.run 一次性? 
**96. B** ?大量拼接?StringBuffer 避免反复创建字符串? 
**97. B** ?默认 Map ?LinkedHashMap（插入顺序）? 
**98. B** ?Queue 先进先出? 
**99. B** ?需 dynamic 类型才能触发 noSuchMethod? 
**100. B** ?`...rest` 收集列表剩余元素? 
**101. B** ?注解类构造函数必?const? 
**102. B** ?顺序 await 累加耗时；Future.wait 并发，总耗时取最慢? 
**103. B** ?Future.any 返回最先完成的 Future 结果? 
**104. B** ?pow 返回 double，要整数需 toInt()? 
**105. B** ?Random 是伪随机，不适合加密? 
**106. B** ?Completer 用于回调?Future、外部事件触发完成? 
**107. B** ?一?Completer 只能 complete 一次，重复会报错? 
**108. B** ?单播 Stream 只能 listen 一次；广播需 asBroadcastStream()? 
**109. C** ?timeout、distinct、transform 等是 Stream 特有? 
**110. B** ?Zone 可捕获异步错误，zoneValues 传递上下文? 
**111. B** ?非空目录 delete 需 recursive: true? 
**112. B** ?`T extends num` 是编译期泛型约束，限?T ?num 子类型?
