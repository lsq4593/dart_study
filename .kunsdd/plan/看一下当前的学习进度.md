做好了！我为你新增了 **4 道综合练习题**（第21~24题），覆盖了之前课程中的核心知识点组合：

---

## 🆕 新练习题一览

### 第21题：数据流水线处理器
| 项目 | 内容 |
|------|------|
| **知识点** | 泛型类、Stream、`sync*` 生成器、Iterable 链式调用、extension、Record |
| **难度** | ⭐⭐ 中等 |
| **需要实现** | `Pipeline<T>` 泛型类、`range()` 生成器、`stats()` Record 函数 |

### 第22题：异步任务调度器
| 项目 | 内容 |
|------|------|
| **知识点** | Future、Completer、Timer、retry 重试、超时降级、Zone 隔离 |
| **难度** | ⭐⭐⭐ 中高 |
| **需要实现** | `retry()` 重试函数、`timeoutWithFallback()` 超时降级、`TaskScheduler` 调度器类、Completer、Zone |

### 第23题：订单状态机 ⭐ 推荐先做
| 项目 | 内容 |
|------|------|
| **知识点** | sealed class、switch 表达式、copyWith、不可变对象、模式匹配、状态转换 |
| **难度** | ⭐⭐⭐ 中高 |
| **需要实现** | `OrderState` sealed class、`Order`/`OrderItem` 不可变类、`formatOrder()`、`tryTransition()` 状态机 |

### 第24题：迷你日志分析器
| 项目 | 内容 |
|------|------|
| **知识点** | 增强枚举、正则表达式、DateTime 解析、StringBuffer、集合统计、extension |
| **难度** | ⭐⭐ 中等 |
| **需要实现** | `LogLevel` 增强枚举、`LogEntry.parse()` 正则解析、extension 统计、分析报告生成 |

---

### 做题方式
```bash
dart run "Test question/第21题_数据流水线处理器.dart"
```

每道题都有完整的 TODO 标注和详细的测试代码，运行后会告诉你期望的输出结果。做完后跟我说 **"给答案"**，我会把参考答案写进去。

你想从哪道题开始做？😄